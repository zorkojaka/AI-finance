export async function seedClients() {
  // Placeholder for future DB seeding logic.
  // In the current mock setup we have no persistence layer,
  // so there is nothing to seed yet.
  return Promise.resolve();
}

